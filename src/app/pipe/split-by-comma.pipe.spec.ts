import { SplitByCommaPipe } from './split-by-comma.pipe';

describe('SplitByCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
